name: azzahraly_motion
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  collection: ^1.18.0
  file_picker: ^6.1.1
  flutter:
    sdk: flutter
  flutter_libserialport: ^0.3.0
  keymap: ^0.0.92
  path_provider: ^2.1.1
  window_manager: ^0.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
      - assets/images/
